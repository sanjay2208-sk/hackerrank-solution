#include <iostream>

using namespace std;

int main() {
    int a, b, c;
    
    // Read three space-separated integers
    if (cin >> a >> b >> c) {
        // Calculate and print the sum
        int sum = a + b + c;
        cout << sum << endl;
    }
    
    return 0;
}

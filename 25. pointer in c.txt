#include <stdio.h>
#include <stdlib.h> // For the abs() function

void update(int *a, int *b) {
    int temp_a = *a;
    *a = temp_a + *b;
    *b = abs(temp_a - *b);
}

int main() {
    int a, b;
    int *pa = &a, *pb = &b;
    
    // Reading input: 4 and 5
    if (scanf("%d %d", &a, &b) == 2) {
        update(pa, pb);
        printf("%d\n%d", a, b);
    }
    
    return 0;
}
